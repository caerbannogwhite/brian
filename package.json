{
  "name": "rough-spreadsheet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development bun run --hot src/server.ts",
    "build": "NODE_ENV=production bun build ./src/index.ts --outdir ./dist --target browser --minify",
    "start": "NODE_ENV=production bun run src/server.ts"
  },
  "devDependencies": {
    "bun-types": "^1.2.15",
    "typescript": "^5.3.3"
  }
}
