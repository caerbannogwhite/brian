/**
 * User-facing version string. Shown in the status bar, the About tab,
 * and the standalone /about page. Release day bumps this once, next to
 * the package.json version; every consumer imports it from here.
 */
export const APP_VERSION = "0.15-my-trusty-servant";
